# secrets, never commit
.env

# python
__pycache__/
*.pyc

# runtime artifacts
data/latency.jsonl
scripts/*.mp3

# bulky design export (kept locally, not in the repo)
stitch_eira_voice_companion_interface.zip

# vendor documentation condensed for local reference only, not ours to redistribute
rime-docs.md
qdrant-docs.md

# internal build notes, kept locally
claude_code_build_brief_EIRA.md
